# Custom Backend OAuth Configuration
# The URL of your backend API that handles Google OAuth
# For local development, use: http://localhost:3001
# For production, use your actual backend URL: https://api.your-domain.com
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# Add other existing environment variables below
# (This is just an example file - copy to .env.local and fill in your values)
